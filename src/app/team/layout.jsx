export const metadata = {
    title: 'Team || CyberDev Solution - Digital IT Services',
    description: 'Empowering businesses with innovative digital IT solutions at CyberDev.',
  }


const Layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default Layout;